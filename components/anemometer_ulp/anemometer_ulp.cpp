#include "anemometer_ulp.h"

// This file intentionally minimal — all logic is in the header.
// It exists so CMakeLists can register it as the ulp_exp_dep_src,
// which ensures ulp_main.h is generated before compilation.
